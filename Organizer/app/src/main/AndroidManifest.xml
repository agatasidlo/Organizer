<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.organizer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_main"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MenuActivity"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".AddToScheduleActivity"
            android:screenOrientation="portrait"
            android:label="@string/Plan_zajec"></activity>
        <activity
            android:name=".ScheduleSubjectListActivity"
            android:screenOrientation="portrait"
            android:label="@string/Plan_zajec"></activity>
        <activity
            android:name=".AddToListActivity"
            android:screenOrientation="portrait"
            android:label="@string/Lista"></activity>
        <activity
            android:name=".CalendarActivity"
            android:screenOrientation="portrait"
            android:label="@string/Kalendarz"></activity>
        <activity
            android:name=".ListActivity"
            android:screenOrientation="portrait"
            android:label="@string/Lista"></activity>
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>